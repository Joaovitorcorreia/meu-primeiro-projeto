import random

answers = [
    'yes',
    'no',
    'maybe'
    'very likely',
    'not sure',
    'do not count on it',
    'definitely',
]
ask = input('Ask the magic 8 ball a question: ')
print(random.choice(answers))